{{- $machineProfile := get . "machine_profile" | default "minimal" -}}
{{ if ne $machineProfile "personal" -}}
.gitconfig-ssh
.ssh/config
{{ end -}}
{{ if ne $machineProfile "work" -}}
.proxy.sh
{{ end -}}
